'use client'

import OrderDetail from '@app/containers/Orders/OrderDetail'
import { Suspense } from 'react'

const OrderDetailPage = () => {
  return <Suspense>{/*<OrderDetail />*/}</Suspense>
}

export default OrderDetailPage
